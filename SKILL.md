---
name: salary-negotiation-group
description: >
  Create a private Lark communication group for a candidate's current stage —
  salary negotiation (谈薪), pending onboarding (待入职), onboarded (已入职),
  pending payment (待回款), etc. Use when the user gives a build-group phrase
  such as "XX人选谈薪，顾问是XX", "XX客户-XX候选人-待入职 顾问XX", "口令建群",
  "给XX建XX群", or mentions creating a group with a candidate stage. Create the
  group named "客户名-候选人姓名-状态" and invite the named consultant.
---

# 候选人状态建群

当用户给出建群口令时，创建一个仅供内部沟通的飞书私密群。发起口令的人作为群主自动入群，再邀请指定顾问；不要默认邀请候选人或客户，以免泄露沟通信息。群名携带候选人当前的状态阶段。

## 解析输入

从自然语言中提取以下字段：

- 客户名
- 候选人姓名
- 状态阶段：谈薪、待入职、已入职、待回款等。口令里出现哪个状态就用哪个（用户可能随时补充新状态，按口令原样使用即可）；口令未给出状态时默认「谈薪」并在结果中注明。
- 顾问姓名

支持以下等价说法：

- `A客户-B人选谈薪，顾问是C`
- `给B建谈薪群，客户A，顾问C`
- `B人选谈薪，顾问是C，客户A`
- `太一 焦大伟 谈薪 Coco`（空格分隔）

客户名、候选人姓名和顾问姓名均为必填，因为它们分别决定群名称和邀请对象。字段缺失时，只询问缺失字段，不创建群。

**客户简称映射**：解析出客户名后，先查本技能目录下 `clients.json` 的 `aliases` 表，命中则用全称替换（群名统一使用全称）；未命中保持用户原词。用户随时可以补充映射（如「太一就是太一量生」「XX就是XX」），新映射回写 `clients.json`，不要问用户「是哪个客户」这类已归档的问题。

## 建群流程

1. 客户名先按 `clients.json` 的简称映射翻译为全称（见「解析输入」），再定群名 `客户名-候选人姓名-状态`（状态取自解析出的阶段，如 `Jully.ai-李爽-待入职`）。确认它不超过 60 个字符；若超长，向用户确认可接受的简称，避免截断造成歧义。
2. 先查顾问表配置（本技能目录下 `consultants.json`），按 `display_name` 或 `full_name` 命中则直接使用其中的 `open_id`，无需搜索。未命中时才通过飞书联系人能力搜索顾问姓名；仅在得到唯一匹配时继续，同名或找不到人时展示可辨识的候选信息并询问用户选择，不创建群。新确认的顾问（姓名、open_id、邮箱）回写进 `consultants.json`，避免下次重复搜索。
3. 搜索群名完全相同的现有私密群。若存在，报告并提供该群，不重复创建。
4. 以**应用机器人身份**（`--as bot`）创建私密普通群，名称使用上述群名；用 `--owner` 把发起口令的用户指定为群主（先查出发起用户的 open_id），并用 `--users` 同时邀请发起用户和已确认的顾问。机器人建群不需要用户额外授权。
5. 读取创建结果，再用群成员查询确认群名、私密类型、群主和顾问已入群。返回群名和群链接；不要默认发送群公告或首条消息。

这是一条明确的执行口令：当三个字段都完整且顾问可唯一识别时，直接建群，无需额外确认。仅在信息不足、人员匹配不唯一、同名群已存在或飞书权限不足时暂停。

## 飞书操作

执行前读取已安装的飞书即时通讯与认证说明，优先使用群聊创建、联系人搜索、群聊搜索和群成员查询的高级命令。使用机器人身份（`--as bot`）创建群，机器人需已具备建群与加人权限；群类型保持 `private`，群模式保持普通群。

**机器人可见性问题**：创建时若 `--users` 直接邀请其他用户失败（错误 232043，机器人对目标用户不可见），改用两步流程——先建群（只含发起用户），再用 `--as bot` 调群成员添加接口（`succeed_type=1`）把顾问加入，无法加入的用户会出现在 `invalid_id_list` 中，如实报告给用户。

如果用户只说“口令谈薪建群”，将其理解为本流程提示，要求补充客户名、候选人姓名和顾问姓名。

## 示例

输入：`太一 焦大伟 谈薪 Coco`

执行：客户名「太一」按 `clients.json` 映射为「太一量生」，创建私密群 `太一量生-焦大伟-谈薪`，群主为发起用户，邀请顾问 Coco（徐静雯）。

输入：`Jully.ai 李爽 待入职。Cassie`

执行：创建私密群 `Jully.ai-李爽-待入职`，群主为发起用户，邀请顾问 Cassie（李婧怡）。

输入：`李然人选谈薪，顾问是王宁`

输出：询问客户名后再执行。
